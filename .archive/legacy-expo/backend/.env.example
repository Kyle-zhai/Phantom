# Copy to .env and fill in
# Get sandbox keys free at https://dashboard.plaid.com/signup
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# Optional: turn on real scrape monitoring (default false uses seed prices)
PRICE_MONITOR_LIVE=false

# Server config
PORT=3000
APP_BUNDLE_ID=com.subspy.app
