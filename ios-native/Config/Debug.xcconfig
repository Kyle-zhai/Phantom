// Debug build. Phantom is fully on-device (Vision OCR + CoreML + SwiftData);
// no backend, no Plaid. If a backend integration lands later, re-add
// PHANTOM_API_BASE / PLAID_ENVIRONMENT here AND in project.yml's Info.plist
// properties so they propagate to Bundle.main.

PRODUCT_BUNDLE_IDENTIFIER = com.yinanzhai.phantom.debug
SWIFT_ACTIVE_COMPILATION_CONDITIONS = DEBUG

DEVELOPMENT_TEAM = 7N337R6J9M
