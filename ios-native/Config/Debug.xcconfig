// Debug build — talks to local Express server.
// Used by Xcode Debug runs in Simulator.

SUBSPY_API_BASE = http:/$()/localhost:3000
PLAID_ENVIRONMENT = sandbox
PRODUCT_BUNDLE_IDENTIFIER = com.subspy.app.debug
SWIFT_ACTIVE_COMPILATION_CONDITIONS = DEBUG
