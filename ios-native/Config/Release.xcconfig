// Release build — used for TestFlight and App Store submissions.
// Phantom is fully on-device (Vision OCR + CoreML + SwiftData); no backend,
// no Plaid. If a backend integration lands later, add PHANTOM_API_BASE /
// PLAID_ENVIRONMENT here AND in project.yml's Info.plist properties so they
// propagate to Bundle.main.

PRODUCT_BUNDLE_IDENTIFIER = com.yinanzhai.phantom
SWIFT_ACTIVE_COMPILATION_CONDITIONS =

DEVELOPMENT_TEAM = 7N337R6J9M
